import 'config.dart';

/// This class helping putting all
/// the urls needed in apps
class AppUrls {
  AppUrls._();

  /// This is the base url for the app
  static String? domain = Config.baseUrl;
}
